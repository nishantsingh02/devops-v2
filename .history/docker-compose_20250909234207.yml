name: Deploy the backend
on:
  push:
    branches: [ main ]
    jobs:
      build:
        runs-on: unbuntu-latest
        steps:
          - name: Checkout the code
            uses: actions/checkout@v2

          - name: Docker login
            uses: 

